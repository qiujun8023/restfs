services:
  restfs:
    image: qiujun8023/restfs:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - ADMIN_TOKEN=${ADMIN_TOKEN}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
