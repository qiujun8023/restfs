services:
  restfs:
    image: ${DOCKER_REGISTRY}/restfs:latest
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - ADMIN_TOKEN=${ADMIN_TOKEN}
    restart: unless-stopped
